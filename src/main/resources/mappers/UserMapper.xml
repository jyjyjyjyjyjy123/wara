<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.wara.mapper.UserMapper">

    <!-- 사용자 등록 -->
    <insert id="insertUser" parameterType="com.project.wara.domain.User">
        INSERT INTO USER_TBL (user_seq, id, password, nickname, profile_image)
        VALUES (user_seq_seq.NEXTVAL, #{userId}, #{password}, #{nickname}, #{profileImage, jdbcType=VARCHAR})
    </insert>

    <!-- 아이디로 회원 조회 -->
    <select id="findById" parameterType="string" resultType="com.project.wara.domain.User">
        SELECT user_seq, id AS userId, password, nickname, profile_image
        FROM USER_TBL
        WHERE id = #{userId}
    </select>

</mapper>
